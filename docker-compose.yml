version: '3'

volumes:
  redis:

services:
  redis:
    image: redis:latest
    volumes:
      - redis:/data
    # command: redis-server --appendonly
  bot:
    build: .
    restart: always
    volumes:
      - .:/usr/src/app
    depends_on:
      - redis
    environment:
      - REDIS_URL=redis://redis:6379
      - TOKEN=NDM1NzE3Njk5MDcxNjM5NTUy.DhvJCg.g1rMnveUufksKwAqhgrSDwcuL6Q
